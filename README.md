# Project 17: Heisenberg model with short and exponential decaying interactions (MPS)

The code in `TensorNets` is taken from the tutorials, with a few changes made (e.g. `calculate_energy` in `d_dmrg.py`)